{
  "name": "monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "./apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.0.3",
    "vercel": "^28.18.3"
  }
}
